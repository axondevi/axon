# Cloudflare Pages _redirects
#
# CF Pages auto-serves matching files (index.html, stats.html, etc.) and
# automatically redirects .html → pretty URL. We only need a catch-all
# for 404 so unknown routes don't fall back to index.html.
#
# Rules are processed in order; first match wins.

# /agent/:slug → loads agent-runner.html which reads slug from window.location
# (file is named *-runner because CF Pages auto-strips .html and serves
# /agent.html at /agent, which would otherwise eat the /agent/* prefix)
/agent/*  /agent-runner.html  200

/*    /404.html    404
